{
  "name": "lumenproject.se",
  "version": "1.0.0",
  "private": true,
  "description": "lumenproject.se — vanilla site, Pages CMS content in-repo, pre-rendered with Eleventy",
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  },
  "dependencies": {
    "gray-matter": "^4.0.3"
  }
}
